{
  "name": "roomate",
  "version": "1.0.0",
  "description": "app para encontrar compañero de cuarto",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch --env-file=.env.dev app.js",
    "prod": "node --watch --env-file=.env.production app.js",
    "json-server": "json-server --watch db.json --port 3001",
    "bd-dev": "docker compose -f docker-compose.dev.yml --env-file .env.dev up -d",
    "bd-prod": "docker compose -f docker-compose.prod.yml --env-file .env.production up -d"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "mongoose": "^8.18.1"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
